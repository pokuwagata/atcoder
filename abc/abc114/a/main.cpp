#include <bits/stdc++.h>
#define rep(i,n) for(int i=0; i < (n); i++)
#define lower(s) transform(s.begin(), s.end(), s.begin(), ::tolower)
using namespace std;
using ll = long long;

int n;
int toInt(vector<int> A) {
  int result = 0;
  for(int d : A) {
    result = result * 10 + d;
  }
  return result;
}

void dfs(vector<int> A) {
  if(A.size() == to_string(n).length()) {
    A.
  }
}

int main() {
  cin >> n;
  vector<int> A = {0};
  dfs(A);

  return 0;
}